<Page x:Class="Workers_base1._0.Viewing_page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Workers_base1._0"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="800"
      Title="Viewing_page">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid x:Name="workersBoard"></DataGrid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Command="{Binding FindDialog}" Grid.Column="0" FontSize="40" Margin="50,50" Background="Orange" Foreground="White">FIND</Button>
            <Button Command="{Binding SaveDataBaseToFile}" Grid.Column="1" FontSize="40" Margin="50,50" Background="Orange" Foreground="White">SAVE TO .txt</Button>
        </Grid>

    </Grid>
</Page>
